/* =========================================================
   PERSONALIZACIÓN RÁPIDA
   Cambia estos datos y listo.
   ========================================================= */

const birthday = {
  name: "Valentina",
  age: 21
};

/* =========================================================
   MENSAJES
   Para agregar otro:
   { from: "Nombre", text: "Mensaje", icon: "💜" }
   ========================================================= */

const messages = [
  {
    from: "Tu familia",
    text: "Aunque estés lejos, queremos que sientas que estamos contigo en este día. Te queremos muchísimo.",
    icon: "🏡"
  },
  {
    from: "Alguien especial",
    text: "Que tus 20 lleguen llenos de experiencias, tranquilidad, nuevos lugares y personas bonitas.",
    icon: "✨"
  },
  {
    from: "Un abrazo desde casa",
    text: "Guarda esta página para esos días en los que necesites escuchar o ver algo que te recuerde cuánto te quieren.",
    icon: "💌"
  }
];

/* =========================================================
   AUDIOS
   Guarda los archivos en: assets/audio/

   Ejemplo:
   {
     name: "Mamá",
     description: "Un mensaje para ti",
     file: "assets/audio/mama.mp3",
     icon: "💜"
   }
   ========================================================= */

const audios = [
  {
    name: "Cristian",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Cristian.ogg",
    icon: "🎙️"
  },
  {
    name: "Daniela",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Daniela.ogg",
    icon: "🎙️"
  },
  {
    name: "Dayana",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Dayana.ogg",
    icon: "🎙️"
  },
  {
    name: "Geronimo",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Geronimo.ogg",
    icon: "🎙️"
  },
  {
    name: "Jeferson",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Jeferson.ogg",
    icon: "🎙️"
  },
  {
    name: "Karina",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Karina.ogg",
    icon: "🎙️"
  },
  {
    name: "Luciana",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Luciana.ogg",
    icon: "🎙️"
  },

  /* VERIFICA EL NOMBRE EXACTO DE ESTE ARCHIVO */
  {
    name: "Lucy y Gero",
    description: "Un mensaje que viajó hasta ti 💜",
    file: "assets/audio/Lucy y Gero.ogg",
    icon: "🎙️"
  },

  /* VERIFICA EL NOMBRE EXACTO DE ESTE ARCHIVO */
  {
    name: "Nona Omaira",
    description: "Un abrazo convertido en audio 💜",
    file: "assets/audio/Nona Omaira.ogg",
    icon: "🎙️"
  },

  /* VERIFICA EL NOMBRE EXACTO DE ESTE ARCHIVO */
  {
    name: "Prima Paula",
    description: "Un mensaje especial para este día ✨",
    file: "assets/audio/Prima Paula.ogg",
    icon: "🎙️"
  },

  {
    name: "Sharith",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Sharith.ogg",
    icon: "🎙️"
  },

  /* VERIFICA EL NOMBRE EXACTO DE ESTE ARCHIVO */
  {
    name: "Tío Michael",
    description: "Un mensaje especial desde la distancia 💜",
    file: "assets/audio/Tío Michael.ogg",
    icon: "🎙️"
  },

  {
    name: "Vilma",
    description: "Un mensaje especial para ti 💜",
    file: "assets/audio/Vilma.ogg",
    icon: "🎙️"
  }
];


/* =========================================================
   VIDEOS
   Guarda los archivos en: assets/video/

   Recomendado para web:
   .mp4 con video H.264 y audio AAC

   Ejemplo:
   {
     name: "Papá y mamá",
     description: "Un saludo desde casa",
     file: "assets/video/familia.mp4",
     poster: "assets/images/portada-video.jpg"
   }

   Si no quieres imagen de portada:
   poster: ""
   ========================================================= */

const videos = [
  {
    name: "Un mensaje para ti 💜",
    description: "Porque incluso desde lejos hay personas celebrando contigo.",
    file: "assets/video/Video1.mp4",
    poster: ""
  },
  {
    name: "Y todavía hay más... ✨",
    description: "Otro pequeño regalo para acompañarte en este día.",
    file: "assets/video/Video2.mp4",
    poster: ""
  }
];

/* =========================================================
   FOTOGRAFÍAS
   Guarda las imágenes en: assets/images/

   Para agregar más fotos solo añade más objetos.
   ========================================================= */

const photos = [
  {
    file: "assets/images/1.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/2.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/3.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/4.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/5.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/6.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/7.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/8.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/9.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/10.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/11.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/12.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/13.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/14.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/15.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/16.jpeg",
    alt: "Un recuerdo especial"
  },
  {
    file: "assets/images/17.jpeg",
    alt: "Un recuerdo especial"
  }
];

/* =========================================================
   ELEMENTOS DEL DOM
   ========================================================= */

const birthdayName = document.getElementById("birthdayName");
const birthdayAge = document.getElementById("birthdayAge");
const openButton = document.getElementById("openButton");
const content = document.getElementById("content");

const messagesContainer = document.getElementById("messagesContainer");
const audioContainer = document.getElementById("audioContainer");
const videoContainer = document.getElementById("videoContainer");
const galleryContainer = document.getElementById("galleryContainer");

const imageModal = document.getElementById("imageModal");
const modalImage = document.getElementById("modalImage");
const closeModal = document.getElementById("closeModal");

const confettiButton = document.getElementById("confettiButton");
const confettiContainer = document.getElementById("confettiContainer");

/* =========================================================
   CARGA INICIAL
   ========================================================= */

birthdayName.textContent = birthday.name;
birthdayAge.textContent = birthday.age;

createStars();
renderMessages();
renderAudios();
renderVideos();
renderPhotos();
setupRevealObserver();

/* La primera tarjeta aparece inmediatamente */
document.querySelector(".hero .reveal")?.classList.add("visible");

/* =========================================================
   ABRIR SORPRESA
   ========================================================= */

openButton.addEventListener("click", () => {
  content.classList.add("visible");

  launchConfetti(90);

  setTimeout(() => {
    document.querySelector(".intro-section").scrollIntoView({
      behavior: "smooth"
    });
  }, 160);
});

/* =========================================================
   MENSAJES
   ========================================================= */

function renderMessages() {
  messagesContainer.innerHTML = "";

  messages.forEach((message) => {
    const card = document.createElement("article");
    card.className = "message-card";

    card.innerHTML = `
      <div class="message-icon">${message.icon}</div>
      <h3>${escapeHTML(message.from)}</h3>
      <p>${escapeHTML(message.text)}</p>
    `;

    messagesContainer.appendChild(card);
  });
}

/* =========================================================
   AUDIOS
   ========================================================= */

function renderAudios() {
  audioContainer.innerHTML = "";

  audios.forEach((audioItem) => {
    const card = document.createElement("article");
    card.className = "audio-card";

    card.innerHTML = `
      <div class="audio-top">
        <div class="avatar">${audioItem.icon || "🎧"}</div>

        <div>
          <h3>${escapeHTML(audioItem.name)}</h3>
          <p>${escapeHTML(audioItem.description || "")}</p>
        </div>
      </div>

      <audio controls preload="metadata">
        <source src="${audioItem.file}">
        Tu navegador no soporta reproducción de audio.
      </audio>
    `;

    audioContainer.appendChild(card);
  });
}

/* =========================================================
   VIDEOS
   ========================================================= */

function renderVideos() {
  videoContainer.innerHTML = "";

  videos.forEach((videoItem) => {
    const card = document.createElement("article");
    card.className = "video-card";

    const posterAttribute = videoItem.poster
      ? `poster="${videoItem.poster}"`
      : "";

    card.innerHTML = `
      <video controls playsinline preload="metadata" ${posterAttribute}>
        <source src="${videoItem.file}" type="video/mp4">
        Tu navegador no soporta reproducción de video.
      </video>

      <div class="video-info">
        <h3>${escapeHTML(videoItem.name)}</h3>
        <p>${escapeHTML(videoItem.description || "")}</p>
      </div>
    `;

    videoContainer.appendChild(card);
  });
}

/* =========================================================
   GALERÍA
   ========================================================= */

function renderPhotos() {
  galleryContainer.innerHTML = "";

  photos.forEach((photo) => {
    const button = document.createElement("button");
    button.className = "gallery-item";
    button.setAttribute("aria-label", `Abrir ${photo.alt}`);

    button.innerHTML = `
      <img src="${photo.file}" alt="${escapeHTML(photo.alt)}" loading="lazy">
    `;

    button.addEventListener("click", () => {
      modalImage.src = photo.file;
      modalImage.alt = photo.alt;
      imageModal.classList.add("open");
      imageModal.setAttribute("aria-hidden", "false");
      document.body.style.overflow = "hidden";
    });

    galleryContainer.appendChild(button);
  });
}

function closeImageModal() {
  imageModal.classList.remove("open");
  imageModal.setAttribute("aria-hidden", "true");
  document.body.style.overflow = "";
}

closeModal.addEventListener("click", closeImageModal);

imageModal.addEventListener("click", (event) => {
  if (event.target === imageModal) {
    closeImageModal();
  }
});

document.addEventListener("keydown", (event) => {
  if (event.key === "Escape" && imageModal.classList.contains("open")) {
    closeImageModal();
  }
});

/* =========================================================
   REVEAL AL HACER SCROLL
   ========================================================= */

function setupRevealObserver() {
  const elements = document.querySelectorAll(".reveal");

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("visible");
          observer.unobserve(entry.target);
        }
      });
    },
    {
      threshold: 0.12
    }
  );

  elements.forEach((element) => observer.observe(element));
}

/* =========================================================
   ESTRELLAS DE FONDO
   ========================================================= */

function createStars() {
  const starsContainer = document.getElementById("stars");

  for (let i = 0; i < 45; i++) {
    const star = document.createElement("span");
    star.className = "star";

    star.style.left = `${Math.random() * 100}%`;
    star.style.top = `${Math.random() * 100}%`;
    star.style.animationDelay = `${Math.random() * 3}s`;
    star.style.opacity = Math.random();

    const size = Math.random() * 4 + 2;
    star.style.width = `${size}px`;
    star.style.height = `${size}px`;

    starsContainer.appendChild(star);
  }
}

/* =========================================================
   CONFETI
   ========================================================= */

confettiButton.addEventListener("click", () => {
  launchConfetti(120);
});

function launchConfetti(amount = 100) {
  const colors = [
    "#9b5de5",
    "#f15bb5",
    "#fee440",
    "#00bbf9",
    "#00f5d4",
    "#ffffff"
  ];

  for (let i = 0; i < amount; i++) {
    const piece = document.createElement("span");
    piece.className = "confetti";

    piece.style.left = `${Math.random() * 100}%`;
    piece.style.background = colors[Math.floor(Math.random() * colors.length)];
    piece.style.animationDuration = `${Math.random() * 2.5 + 3}s`;
    piece.style.animationDelay = `${Math.random() * 0.8}s`;
    piece.style.setProperty(
      "--drift",
      `${Math.random() * 240 - 120}px`
    );

    const scale = Math.random() * 0.8 + 0.5;
    piece.style.transform = `scale(${scale})`;

    confettiContainer.appendChild(piece);

    setTimeout(() => {
      piece.remove();
    }, 6500);
  }
}

/* =========================================================
   UTILIDAD BÁSICA PARA TEXTO
   ========================================================= */

function escapeHTML(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}
